language: ruby

sudo: required

dist: precise

cache:
  bundler: true
  edge: true

rvm: default

notifications:
  email: false

script:
  - mkdir log
  - cat ~/build.sh

addons:
  artifacts:
    paths:
    - log/
    branch:
      - 'master'
